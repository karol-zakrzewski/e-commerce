import { Color } from "@/components/shapes/types";

export const colorsMap: Record<Color, string> = {
  orange: "bg-gradient-to-br from-orange-500 to-orange-100",
  blue: "bg-gradient-to-br from-blue-500 to-blue-200",
};
